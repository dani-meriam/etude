import { OnChanges, SimpleChanges } from '@angular/core';
import { Pane } from '../pane';
import { SliderComponent } from '../slider/slider.component';
import { ViewStrategy } from '../view-strategies/view-strategy';
import { ViewComponent } from '../view/view.component';
export declare class GridComponent implements OnChanges {
    readonly slider: SliderComponent;
    readonly parentView: ViewComponent;
    private readonly viewStrategy;
    pane: Pane;
    currentTimestamp: number;
    selectedTimestamp: number;
    minTimestamp: number;
    maxTimestamp: number;
    itemFormat: string;
    itemLabels: string[];
    readonly _hostClass: string;
    constructor(slider: SliderComponent, parentView: ViewComponent, viewStrategy: ViewStrategy);
    ngOnChanges(changes: SimpleChanges): void;
    makeItemClasses(index: number, pane: Pane): string;
    isDisabled(index: number, pane: Pane): boolean;
    trackIndex(index: number): number;
}
